@* Small uppercase section label used across enterprise forms. *@
<div style="color:#475569; font-size:0.72rem; font-weight:700; letter-spacing:1.5px; text-transform:uppercase; margin-bottom:4px;">
    @Text
</div>

@code {
    [Parameter] public string Text { get; set; } = "";
}
